<%- include('partials/headerSignOut') -%>
<div class="window d-flex justify-content-center">
    <div class="profile-bg">
        <div class="row whole-profile">
            <div class="col-3">
                <div class="profile-left">
                   <span class="profile-pic"></span>
                    <h4><%= username %></h4>
                    <h4><%= email %></h4>
                    <% if (favCui === undefined) { %>
                        <br>
                    <% } else { %>
                        <h4>My Favourite Cuisine:</h4>
                        <a href="http://localhost:4000/<%= favCui %>"><%= favCui %></a>
                    <% } %>
                    <% if (diet === undefined) { %>
                        <br>
                    <% } else { %>
                        <h4>My Diet Preference:</h4>
                        <a href="http://localhost:4000/<%= diet %>"><%= diet %></a>
                    <% } %>
                </div>
            </div>
            <div class="col-9">
                <div class="profile-right">
                    <div class="row">
                        <div class="col-6">
                            <h3>My Recipes</h3>
                            <% if (recipeArr.length === 0) { %>
                                <p>You haven't added any recipes yet!</p>
                                <p>Share your recipes <a href="http://localhost:4000/add-recipe">here</a></p>
                            <% } else { %>
                                <% for (let i = 0; i < recipeArr.length; i++) { %>
                                    <div class="row">
                                        <div class="col-4">
                                            <img src="<%= recipeArr[i]['recipe_image'] %>" alt="<%= recipeArr[i]['recipe_name'] %>" class="added-recipe-photo">
                                        </div>
                                        <div class="col-8 d-flex align-items-center">
                                            <!-- <a href=""></a> what should be the link ??????? -->
                                            <p><%= recipeArr[i]['recipe_name'] %></p>
                                        </div>
                                    </div>
                                <% } %>
                            <% } %>
                        </div>
                        <div class="col-6">
                            <h3>
                                My Favourite Recipes
                            </h3>
                        </div>
                    </div>                    
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') -%>